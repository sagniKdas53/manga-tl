inputs/
.claude/
.agents/
data/
logs/
*__pycache__
.venv/
.env
.idea
# AGENTS.md
# CLAUDE.md
.antigravitycli/*
secrets/
frontend/coverage
# examples/ moved to corpus/examples (in the manga-tl-corpus submodule) — it is the source
# every corpus is derived from, so it now lives beside them. Gitignored there too, and it
# must stay that way: third-party copyrighted scans, some of it adult content.
.coverage
pytest*
exported_minio/*
exported_images/*

# Benchmark corpora and run output now live in the manga-tl-corpus submodule at corpus/.
# They are derived from examples/ (gitignored and purged from history here), so they have no
# source this repo can track — but they are still worth versioning, because ground truth is
# expensive and a silent change to it is exactly the kind of regression that is otherwise
# invisible. Hence: versioned, separately. See corpus/README.md and docs/benchmarks_guide.md §5.
# The durable role/provenance record stays here: corpus/scripts/examples_manifest.json.
# Papers and research reports live in docs/ for reading, but are not tracked: this repo is
# public, and third-party papers are not ours to redistribute. Cite the DOI in the doc instead.
docs/*.pdf
corpus/.claude
scripts/corpus
docs/reference/mask-eval-candidates-2026-08-26.png
docs/reference/2026-09-06-torii-comparison/*.png
docs/reference/2026-09-06-torii-comparison/*.jpg
docs/reference/2026-09-06-torii-comparison/*.jpeg
docs/reference/2026-09-06-torii-comparison/*.torii
backend/*

# There is now a package.json at the repo root, holding exactly one dependency: playwright, for
# scripts/playwright/*.cjs. Node resolves `require("playwright")` upward from the script, so the
# install has to land here rather than in frontend/. Committed so the corpus run is reproducible;
# the tree it installs is not.
node_modules/

# G0 closure retains machine-readable manifests and decisions; browser exports, source-derived
# project archives, snapshots, and the superseded draft remain local evidence only.
/docs/quality-runs/g0-20260913-closure/canary-sample7/a04-exports/
/docs/quality-runs/g0-20260913-closure/continuation/a04-exports/
/docs/quality-runs/g0-20260913-closure/A09-evaluation-correction-v2.draft.json

# Quality-run artifacts (images, JSON, project zips, logs) stay on disk and on the
# pi5 mirror but are not tracked here; the notes (.md) and harness scripts are.
docs/quality-runs/**/*.png
docs/quality-runs/**/*.jpg
docs/quality-runs/**/*.jpeg
docs/quality-runs/**/*.webp
docs/quality-runs/**/*.svg
docs/quality-runs/**/*.json
docs/quality-runs/**/*.jsonl
docs/quality-runs/**/*.zip
docs/quality-runs/**/*.log
docs/quality-runs/**/*.txt
docs/quality-runs/**/source-served
docs/quality-checkpoints/**/*.png
docs/quality-checkpoints/**/*.jpg
docs/quality-checkpoints/**/*.jpeg
docs/quality-checkpoints/**/*.webp
docs/quality-checkpoints/**/*.svg
docs/quality-checkpoints/**/*.json
docs/quality-checkpoints/**/*.jsonl
docs/quality-checkpoints/**/*.zip
docs/quality-checkpoints/**/*.log
docs/quality-checkpoints/**/*.txt
docs/quality-checkpoints/**/source-served
docs/quality-evidence/**/*.png
docs/quality-evidence/**/*.jpg
docs/quality-evidence/**/*.jpeg
docs/quality-evidence/**/*.webp
docs/quality-evidence/**/*.svg
docs/quality-evidence/**/*.json
docs/quality-evidence/**/*.jsonl
docs/quality-evidence/**/*.zip
docs/quality-evidence/**/*.log
docs/quality-evidence/**/*.txt
docs/quality-evidence/**/source-served

# Torii reference captures saved in the repo root
*.torii
/Upload and Translate Manga Images*
/translated_images_*.zip
docs/quality-runs/r3-20260920-sanity/a04-exports/sample83/suggest.xcf
docs/quality-runs/glyph-erasure-candidates/glyph-erasure-results-torii/images/*
