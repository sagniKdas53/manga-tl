# H01a: basic ownership feature provenance

Status: **ACTIVE**.

## Implemented boundary

`worker.services.ownership_features` now creates deterministic fragment IDs, source quads, source-space bounds, major-axis angle, and declared-style-or-unknown provenance. `OcrCapture.fragment_features` retains the same records and its replayed fragment IDs match them.

A user-authorized nullable `ocr_regions.ownership_provenance jsonb` migration is added. The Rust OCR callback accepts `ownershipProvenance` without exposing it in the public frontend `OcrRegion` type; GitNexus showed that public type has HIGH fan-out, so H01a deliberately avoids a UI/API contract expansion.

The YOLO raw-fragment route now creates source-linked provenance without requiring `OCR_CAPTURE_DIR`; `merge_ocr_regions` preserves it under final-region `ownershipProvenance.fragments`, with explicit container resolution. This adds no cleanup authorization and does not enable F01 owner vetoes.

## Partial status

The non-YOLO/legacy OCR route and a live isolated-stack callback-to-database persistence trace are still required before H01a can be marked DONE. F01/F04 remain blocked; no six-page ownership result is claimed.

## Focused checks

```text
cd worker
../.venv/bin/python -m pytest -q tests/test_merge_regions.py tests/test_ocr_grouping.py tests/test_ocr_capture.py
11 passed
../.venv/bin/python -m ruff check src/worker/services/ownership_features.py src/worker/services/ocr_capture.py src/worker/services/merge_regions.py src/worker/handlers/ocr.py
All checks passed

cd ../backend-rust
cargo check -q
passed
```

## Isolated migration evidence

An ephemeral PostgreSQL 15 container on `127.0.0.1:55432` created a baseline `ocr_regions`, applied the equivalent `ownership_provenance jsonb` DDL, inserted nested fragment provenance, and selected `fragment-test` back through JSONB paths. The container was stopped and removed immediately. This proves JSONB migration/read semantics only; it is not a backend callback trace.

```text
CREATE TABLE
ALTER TABLE
INSERT 0 1
fragment_id: fragment-test
source_style: null
```
