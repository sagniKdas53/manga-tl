# A06-C0: conventional-page source selection

Status: DONE (source curation only; A06-C baseline/labels and G0 remain open).

Date: 2026-09-10. App `d6bd527`; worker `7d70b64`; corpus `ed9b191`.

## Scope and result

The user requested stronger traditional bubble/panel manga, manhua and webcomic representation, plus a language/style gap audit. Reviewed the six-regression selection, inventoried and decoded 637 active/pending source entries, screened all 16 source contact sheets, and reviewed 24 selected controls in six enlarged sheets. Added eight JA, eight KO and eight ZH development controls, preserving all six original regressions.

Evidence: [coverage review](../quality-evidence/style-coverage-review.md), [recipe/exclusions](../quality-evidence/style-coverage-selection.json), [hashed source manifest](../quality-evidence/style-coverage-20260910/resolved-selection.json), [audit summary](../quality-evidence/style-coverage-20260910/summary.json).

The new controls include 20 panel/strip pages, three single-panel balloon pages and one explicitly labelled scene collage. Nineteen have clean gutters. Corpus counts are metadata-declared: 211/29/22 active JA/KO/ZH and 24/192/159 pending. Six wrong-language sources and one unresolved case are excluded from evaluation selection pending a separate corpus role/metadata repair. One exact active/pending duplicate is recorded.

## Impact and preservation

Only parent documentation, new standalone evidence tooling and generated metadata changed. No API routes, application behavior, worker modules, corpus metadata, source pixels, historical stage outputs or submodule pointers changed. GitNexus impact was attempted for the new script and `audit_style_coverage`; neither is indexed, yielding UNKNOWN risk/no graph caller results. Source inspection establishes that this is an isolated opt-in script with no existing application callers. No reindex was needed for a data-curation packet.

## Review limits and gate status

Coordinator source review covers composition and visible dialogue scripts, not ground-truth transcription, ownership, glyph masks, source-action labels, creator identity or output correctness. New controls have no fresh pipeline baseline from this packet. No paid models or production services were invoked. Full long-scroll episodes, verified traditional print manhua and several genre/physical-scan styles remain gaps. Equal language counts do not establish source-family diversity.

A01-A06 historical completions remain unchanged. This child curation packet does not pass A07, A09, G0 or any later milestone. The 24 controls are **development-only**, not a reserved/frozen holdout. A09 must still obtain reviewed, independent language/style strata before tuning.

## Validation

Source inventory/selection generation and a separate hash-pinned rerun both passed: 637 decoded entries, zero source errors, 24 unique selected source hashes and 8/8/8 declared languages matching the visually reviewed dialogue. Recipe/resolved-row equality, style/lane/tag summaries and local links passed; see the [validation record](../quality-evidence/style-coverage-20260910/validation.json). Ruff lint/format and `git diff --check` passed. Initial 120-second inventory timeout was resolved by a 600-second allowance. GitNexus detected LOW-risk documentation changes/no affected execution flows; new untracked evidence/tooling was reviewed separately. Corpus and worker status checks are clean. Application suites and paid pipeline runs were not executed or claimed as passes.

## Resume

Run A06-C as bounded per-fixture packets, starting with `sample7` from the hashed selection. Reuse the isolated A03/A04 capture approach for fresh source-to-stage artifacts and editor/export evidence, then draft/review owners as in A06. Repeat for the remaining selected controls without processing unrelated corpus pages. Continue A07 action/mask/style annotations over both original regressions and the controls. Do not start an algorithm fix or claim a quality improvement from this source selection alone.

Preserve the current six retained chapters/evidence, all unresolved A06 labels, all immutable corpus sources, the new hashed selection and local ignored source sheets. The detailed reproduction commands and validation scope are in the linked review.
