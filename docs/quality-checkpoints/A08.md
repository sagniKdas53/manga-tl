# A08: PDF-derived synthetic scene inputs
Status: DONE

Date / app head / worker head / corpus head: 2026-09-11 / `e7b811040f6b37bc28a9b4a1f8fbf995f2bacca4` / `7b54f8c1c516eb445e869b2776ffa62e4ee2762b` / `ed9b191da255e4d3d132a2d050ab8fffa1815e26`.

Allowed files and actual changed files: `docs/quality-evidence/a08-pdf-derived-scene-fixtures.json`, this checkpoint, and the tracker status update. No application, worker, backend, schema, source image, baseline artifact, submodule, or production symbol changed.

Prerequisites and input artifact hashes: A05 is complete. The visual trigger is `corpus/gaps/unfiled/torii/translated_images_en_gpt-5.6-luna.pdf`, SHA-256 `62fa33f5be220844a892f1b0ffaddc4ced49c54719777326fd17863666ff93bd`; it has two 1109.33 x 1664 point pages. `canvas-zip-results.json` records the earlier source-level fractional restore failure for 186.43 x 187.91 at 12.5 degrees.

Current-checkout reproduction (command, observed result): `node docs/quality-evidence/canvas-zip-fractional-repro.mjs` remains the deterministic source-level reproduction. It serializes 186.43 x 187.91 at 12.5 degrees and models the current archive restore as 150 x 80 at the retained 12.5-degree angle. It is not a live import or a rendering-quality pass.

GitNexus impact (repo, symbols, callers/processes, risk, freshness): A08 adds evidence only and changes no symbol. The related `accepts_the_fractional_box_a_rotation_produces` test was inspected with fresh GitNexus impact: 0 direct callers, 0 affected processes, LOW risk. The result does not authorize an archive behavior change.

Implementation decision and schema/config/model/font versions: `a08-pdf-derived-scene-fixtures/v1` is deliberately a pre-contract fixture format. It defines five deterministic source-space inputs: fractional rotated geometry, whitespace-only automatic text versus explicit manual cleanup, padded normal selection, narrow overlapping independent owners, and overlapping active cleanup. It has no renderer, font, model, cleanup provider, API, or archive-version selection.

Validation commands and executed/pass/fail/not-executed counts:

```text
jq -e '.format == "a08-pdf-derived-scene-fixtures/v1" and (.fixtures | length == 5)' docs/quality-evidence/a08-pdf-derived-scene-fixtures.json
jq -e '[.fixtures[].assertions[] | select(.id == "archive-round-trip-width" or .id == "archive-round-trip-height" or .id == "archive-round-trip-angle" or .id == "automatic-blank-has-no-plate" or .id == "manual-cleanup-remains-visible" or .id == "normal-selection-handles" or .id == "no-owner-merge" or .id == "overlap-is-explicit")] | length == 8' docs/quality-evidence/a08-pdf-derived-scene-fixtures.json
node docs/quality-evidence/canvas-zip-fractional-repro.mjs
git diff --check
PASS: fixture structure and all required measurable cases; source-level fractional-loss reproduction; tracked-diff whitespace.
FAIL: 0. NOT-EXECUTED: no browser, renderer, live API, archive import, or cleanup implementation exists for this pre-contract evidence task.
```

Before/after metrics and artifact paths: the manifest contains five fixtures and 18 named assertions. `fractional-rotated-caption` fixes the exact 186.43 x 187.91 / 12.5-degree acceptance values. `blank-automatic-and-manual-cleanup` requires zero automatic changed pixels for whitespace-only text and one intentional manual cleanup. The narrow-owner fixture has two separate owners despite a 12px container overlap; the cleanup fixture has a measurable 30 x 39px overlap.

Quality-gate result and reviewer: A08 task gate is complete: the synthetic inputs encode measurable behavior without claiming that the PDF screenshots expose original editable geometry. A08 does not pass G0. A09 must freeze a reviewed independent language/style/source-family holdout before G0 can be evaluated.

Remaining uncertainty or blocker: these fixtures are not yet B01 contract fixtures and cannot validate current archive behavior. Their G7 save/reload and H05/H06 editor assertions remain future acceptance work. The fractional fixture intentionally describes required new-format behavior, while current source-level archive replay remains a known failure.

Exact next command and expected result: start A09 by inventorying candidate source pages against `docs/quality-evidence/style-coverage-review.md`, excluding the 30 development fixtures and related/near-duplicate families, then save a coordinator-reviewed 24-page, 8-per-language holdout manifest with thresholds and unresolved-label blockers.

Files/worktree that must be preserved: all A01-A07 evidence and checkpoints, both PDF artifacts, the immutable corpus sources, the A08 fixture manifest, and existing submodule revisions.
