# C02-R3: logical scene next revision

Status: **DONE**. `put_page_scene` accepts a new immutable logical snapshot only at `current_revision + 1`, atomically advances the page in the same transaction, and permits a same-digest retry at the now-current revision. A different digest at that revision remains `409`.

Focused isolated-stack gate passed: `cargo test --test pages_endpoints page_scene_api_round_trips_the_new_format_contract`; `cargo fmt --check` passed and test services were removed.
