# G3 — SFX policy boundaries

## Status

**PASSED — request/object-boundary gate only, 2026-09-14.**

Machine-readable evidence: [`G3-policy-evidence.json`](../quality-runs/g3-20260914-policy/G3-policy-evidence.json).

## Evidence

- D01 joins all **257/257** explicit development OCR labels without reading A09. Classifier SFX precision is 1.0 but recall is 15.09%; its result is therefore evidence for review, never permission to alter source pixels. Vertical dialogue, decorated dialogue, and the reproduced confidence-0 control are retained.
- D02 resolves every unreviewed/unknown classification to `review`; only a valid explicit per-region override can select `replace`. Ambiguous dialogue is consequently reported as unresolved/policy-skipped, not translated.
- D03's captured provider boundary has two regions: one explicit-replace dialogue and one classifier-SFX review. The provider target list and repeated page manifest contain the dialogue only; zero preserved-SFX IDs/raw entries are sent. The observed selected chunk count is one.
- D04 proves that the same review SFX never enters batch retry, retry batch, individual fallback, or per-region QA metadata. The selected dialogue remains retryable. Page-wide VLM image input remains complete; only per-region verdict IDs are filtered.
- D05 applies the same effective-`replace`/nonblank rule to cleanup and automatic-text construction in both live Rust scene API validation and worker render-request validation. Contradictory preserve cleanup, blank automatic text, and blank override fixtures are rejected.

## Token accounting

No external provider request was issued: provider calls were mocked at the boundary. External input/output tokens are therefore measured as **0/0** for this gate. Selected-dialogue live token usage is **not executed**, not estimated from number of regions or chunks; M3 is explicitly before paid work.

## Verification

```text
worker focused policy tests: 27 passed
worker full suite: 515 passed
worker pyright: 0 errors, 0 warnings
worker ruff check: passed
changed worker files: 14 formatted
JavaScript page-scene contract: PASS
Rust page-scene contract: 2 passed
```

The whole-worker `ruff format --check .` still reports the pre-existing, untouched `src/worker/schemas.py`; all M3-modified files are formatted.

## Boundary of this pass

This gate does **not** claim source-pixel invariance, cleanup-image correctness, browser-render correctness, provider translation quality, or A09 quality. Those require the later G6/G8/J01 integration gates.
