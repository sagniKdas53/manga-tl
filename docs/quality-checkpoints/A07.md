# A07: reviewed region-action, glyph-support, and source-style coverage
Status: DONE

Date / app head / worker head / corpus head: 2026-09-11 / `885f0d730c5c39a3ac208dad0499d4816a8db344` / `7b54f8c1c516eb445e869b2776ffa62e4ee2762b` / `ed9b191da255e4d3d132a2d050ab8fffa1815e26`.

Allowed files and actual changed files: the 28 newly retained A07 packet JSON/checkpoint pairs for uncommitted fixtures, `docs/quality-runs/a07-20260911-coverage/A07-coverage-audit.json`, this checkpoint, and the tracker update to follow. No source image, baseline artifact, application, worker, backend, schema, model, or submodule changed.

Prerequisites and input artifact hashes: A06-C supplies 24 conventional-control owner sets (including separate sample7 evidence); A06 supplies six original-regression label sets. Every packet pins its own source digest and snapshot. The aggregate audit accounts for 30 fixture packets, 267 existing A06/A06-C label records, and the declared source hashes.

Current-checkout reproduction (command, observed result): ran a cross-packet JSON aggregation over `docs/quality-runs/a07-20260911-*/A07-region-action-glyph-style-labels.*.reviewed.json`. It found 30 unique samples: 14 JA, 8 KO, and 8 ZH. Actions are 172 replace candidates, 53 preserve, 5 explain, and 37 explicit pixel-preserving review decisions. No cleanup or text artifact was generated.

GitNexus impact (repo, symbols, callers/processes, risk, freshness): not applicable. A07 is evidence-only and changes no production symbol, API, worker, route, source, or submodule. Individual packets record the same conclusion.

Implementation decision and schema/config/model/font versions: curation-only `a07-region-action-glyph-style-labels-reviewed/v1` and `a07-coverage-audit/v1`. A replace decision remains a candidate pending later policy, source-space glyph support, and validated cleanup. Preserve/review authorize no automatic cleanup or replacement object; explain is note-only. No provider, font, model, contract field, or renderer was selected.

Validation commands and executed/pass/fail/not-executed counts:

```text
jq -s 'aggregate counts' docs/quality-runs/a07-20260911-*/A07-region-action-glyph-style-labels.*.reviewed.json
PASS: 30 packets, 267 labels, JA=142 / KO=64 / ZH=61, actions replace=172 / preserve=53 / explain=5 / review=37

jq -s -e 'length == 30 and unique source sample count == 30' A07 packet JSON files
PASS: 30 of 30 unique packet source IDs

jq expected six-original source IDs A06-owner-labels.draft.json
jq expected remaining 23 conventional source IDs A06-C-owner-labels.controls.reviewed.json
PASS: source inventories match; sample7 is deliberately held in its own reviewed A06-C label file

jq empty A07 packet JSON files
git diff --check
PASS: JSON parse and tracked-diff whitespace / FAIL: 0 / NOT-EXECUTED: 0
```

Before/after metrics and artifact paths: [aggregate audit](../quality-runs/a07-20260911-coverage/A07-coverage-audit.json) records all fixture IDs, language slices, action totals, required coverage, and gaps. It includes 24 conventional controls and six originals. Sample150-r005 supplies the outlined-glyph protected-art review case; sample7/sample47 retain SFX preservation coverage.

Quality-gate result and reviewer: A07 task gate is complete: reviewed labels exist across JA/KO/ZH for ordinary balloon controls, dialogue/SFX policy, source style, protected art, and an outlined-glyph review case. The 37 review actions remain visible and source-preserving. A07 does not pass G0; A08 and A09 remain, and G0 must retain unresolved hypotheses.

Remaining uncertainty or blocker: sample61 still has 50 explicitly unassigned final OCR regions because retained A06 evidence has no safe fragment/container membership. Sample177 has six illustration labels without final OCR IDs. No review action may be reported as translated or become cleanup. Glyph supports remain annotations only until G01; no automatic patch/text object is authorized by A07.

Exact next command and expected result: start A08, the now dependency-satisfied PDF-derived synthetic scene-input task, in a separate bounded packet. It must create measurable fixtures only and cannot claim source geometry from screenshots.

Files/worktree that must be preserved: all A01-A06-C evidence; every `docs/quality-runs/a07-20260911-*/` packet; every A07 per-fixture checkpoint; the aggregate audit; immutable sources; and the existing submodule revisions.
