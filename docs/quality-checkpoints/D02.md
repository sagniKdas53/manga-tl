# D02: choose region action before translation

Status: **DONE**

Date / app head / worker head / corpus head: 2026-09-14 / `09e4202427535c2ee5e7c6144afe79100008f9a4` / `5a978680950d9c4519064aa4c943878802613927` / `27de7499f0c4c142763116bfcbcfd0afd9709cbf`.

Allowed files and actual changed files: D02 permits one pure worker policy module, one classification seam, focused tests, and this checkpoint. Added `worker/src/worker/services/region_policy.py` and `worker/tests/test_region_policy.py`; updated `worker/src/worker/handlers/layout.py` and `worker/tests/test_ml_handlers.py`; added this checkpoint. No backend schema/API, OCR data, source pixels, A09 artifact, or cleanup renderer changed.

Prerequisites and input artifact hashes: D01's development-only evaluation and frozen rules are at `docs/quality-runs/d01-20260914-classifier/D01-classifier-evaluation.json` and `docs/quality-checkpoints/D01.md`. B04 pins the independently packaged `page-scene/v1` policy actions.

Current-checkout reproduction (command, observed result): the existing layout callback returned only `{regionId, regionType}`. It had no policy action/reason/uncertainty surface, so a downstream stage could not distinguish a descriptive SFX candidate from destructive authorization.

GitNexus impact (repo, symbols, callers/processes, risk, freshness): worker index was current at `5a97868`. Upstream impact for `process_layout` is **LOW** with zero graph callers/processes (the queue entry is graph-unresolved); its focused handler test covers the callback payload. `region_policy.py` is a new module with no preexisting blast radius. No HIGH/CRITICAL finding.

Implementation decision and schema/config/model/font versions: `select_region_action(region_type, user_override)` is pure and returns immutable `RegionPolicy`. Valid explicit `user_override` (`preserve`, `explain`, `replace`, `review`) wins only for that region. Every unreviewed classifier type, including `sfx`, resolves to uncertain `review`; malformed override also fails closed to `review`. `process_layout` emits the stable callback fields `policyKind`, `policyAction`, `policyReason`, `policyOverride`, and `policyUncertain` with the region type. No model/font/config changed.

Validation commands and executed/pass/fail/not-executed counts:

```text
../.venv/bin/python -m pytest -q tests/test_region_policy.py tests/test_ml_handlers.py tests/test_layout_extra.py
PASS: 25 tests. Covers all known/unknown kinds, valid/invalid override, callback surface, classification regression, and handler payload.

../.venv/bin/python -m ruff check src/worker/services/region_policy.py src/worker/handlers/layout.py tests/test_region_policy.py tests/test_ml_handlers.py
../.venv/bin/python -m ruff format --check src/worker/services/region_policy.py src/worker/handlers/layout.py tests/test_region_policy.py tests/test_ml_handlers.py
PASS: zero lint findings; four files formatted.
```

Before/after metrics and artifact paths: no request, provider, retry, OCR, source-pixel, cleanup, or text-object action ran. Before: no region action was emitted. After: every layout-classified region receives exactly one explicit policy decision; absent or invalid authorization is `review` and remains source-preserving.

Quality-gate result and reviewer: D02 task gate is **DONE**. Its policy matrix implements the D01 safety rule, but translation/retry/QA/object boundaries are not wired until D03–D05; G3 remains unexecuted.

Remaining uncertainty or blocker: the current backend layout callback persists only region type, not the added policy fields. D03 deliberately recomputes the same pure decision at its actual target-selection boundary so no persisted legacy state is trusted. This packet does not create page-scene owners or cleanup artifacts.

Exact next command and expected result: start D03 at `worker/src/worker/handlers/translation.py`; captured provider payload must contain neither policy-skipped IDs nor their raw repeated-manifest entries.

Files/worktree that must be preserved: D01 evidence, frozen A09 artifacts, A06/A06-C/A07 evidence, and the new `region_policy.py` action semantics.
