# B03: backend page-scene serialization

Status: **DONE** — the Rust backend has a strict standalone validation/mapping slice for `page-scene/v1`. It exposes no API route yet; B05 owns read/write endpoint wiring.

Date / app head / worker head / corpus head: 2026-09-14 / `473deb2d1dd9ad96f038a5629180874ba6709c57` / `7b54f8c1c516eb445e869b2776ffa62e4ee2762b` / `27de7499f0c4c142763116bfcbcfd0afd9709cbf`.

Allowed files and actual changed files: `backend-rust/src/models.rs`, new `backend-rust/src/page_scene.rs`, the crate export and `backend-rust/tests/page_scene_contract.rs`. The pre-existing `Page` row now includes B02's non-null `scene_revision`; B02's schema smoke test proves fresh `RETURNING *` shape remains available. No HTTP route, database query, source file, output, historical archive, G0 evidence, A09 label/reserve or corpus data changed.

Prerequisites and input artifact hashes: B01 `page-scene/v1` schema/fixtures at app `614e2eb841bb1ef4324563d58f6339ebc4a3662e`; B02 storage at app `473deb2d1dd9ad96f038a5629180874ba6709c57`. The mapper's digest is SHA-256 over the contract's logical projection and validates resolved-scene identity.

Current-checkout reproduction (command, observed result): `cargo test --test page_scene_contract` consumes all three B01 valid fixtures directly, validates then serde-round-trips each JSON scene, and applies every shared invalid mutation. It accepts all valid forms and rejects all nine invalid forms. No old DTO, project state or compatibility reader is invoked.

GitNexus impact (repo, symbols, callers/processes, risk, freshness): upstream impact for `Page` and `Job` in `backend-rust/src/models.rs` reported zero direct callers, zero affected processes and **LOW** risk. No high/critical result occurred. LSP has no configured server, so the repository graph plus exact construction search were used; `Page` has no Rust struct literal caller and `Job` has no Rust struct literal caller.

Implementation decision: `ValidatedPageScene` retains the immutable JSON document, contract/source/logical-scene identity, normalized owner/action/override rows and normalized asset rows. `snapshot_for_page` deliberately requires the API/database UUID boundary to be supplied by B05, preventing an opaque fixture/page ID from silently becoming storage identity. Root unknown fields, unsupported versions, malformed page identity/revision, non-finite geometry, policy/cleanup conflicts, empty automatic text, missing assets, source mismatch, logical/resolved mixing and incorrect resolved digest fail before mapping. New `FromRow` database entities describe B02's snapshot/owner/asset/render-job tables without route-side aliases.

Validation commands and executed/pass/fail/not-executed counts:

```text
cargo fmt && cargo test --test page_scene_contract
PASS: 2/2 tests.
  accepts: 3/3 shared valid fixtures and their serde round trips.
  rejects: malformed non-finite JSON plus 8/8 shared mutation cases.
```

Before/after metrics and artifact paths: before, no backend page-scene DTO or mapper existed. After, one strict mapper, four database row DTOs, three write DTOs and one two-test shared-fixture suite exist under `backend-rust/`. No new dependency was added.

Quality-gate result and reviewer: B03 complete; coordinator implementation review recorded here. G1 is **not executed**: B04 needs an independently packaged worker consumer and B05 must prove the live API/generated TypeScript round-trip.

Remaining uncertainty or blocker: B03 intentionally does not serialize rows over HTTP, enqueue a renderer job or write storage. B05 must bind the route page UUID to `source_page_id`, persist snapshot/owners/assets atomically and reject version/source/digest mismatch before a queue record is created.

Exact next command and expected result: work in the `worker/` submodule; run GitNexus impact against the standalone worker index before touching `schemas.py`, package a non-symlink schema copy plus artifact validator, and run the shared fixture suite from a copied worker-only directory.

Files/worktree that must be preserved: B01 contract artifacts; B02 additive migration and ledger; B03 mapper/fixture tests; frozen G0/A09 evidence and all explicitly unresolved identities.
