# F02: bounded graph components and owner vetoes

Status: **DONE**.

## Change

`GroupingConfig.component_max_members` is an opt-in full-component limit. `GroupingContext.owner_veto` is an opt-in callback evaluated only after connected-component traversal. A non-null F01 decision reason or a component above the configured cap splits that entire component into singleton candidates.

This preserves the frozen default: both fields are off, so `group_fragments` remains legacy-equivalent until F04 supplies an owner-aware context. The boundary cannot create a merge. It sees the complete transitive component, so a middle fragment cannot turn an F01 unknown owner decision back into an A–B–C merge. It deliberately does not use panels, conversations, or reading order.

## Validation

```text
cd worker
../.venv/bin/python -m pytest -q tests/test_fragment_grouping.py tests/test_ocr_grouping_wiring.py tests/test_merge_regions.py
49 passed
../.venv/bin/python -m pyright src/worker/services/fragment_grouping.py src/worker/services/owner_assignment.py
0 errors, 0 warnings
../.venv/bin/python -m ruff check src/worker/services/fragment_grouping.py tests/test_fragment_grouping.py tests/test_owner_assignment.py
All checks passed
```

The component tests now call `assign_captured_owners` with actual quads, source scale, declared-style evidence, and detector containers—rather than a stub callback. An A–B–C bridge and close independent labels produce F01's `different-validated-containers` reason and split. A labelled true unit in one container produces an F01 assignment and stays joined.

[Machine-readable evidence](../quality-runs/f02-20260916-owner-decisions/evidence.json) records two prevented **synthetic** false merges and zero synthetic false splits. It explicitly records that development-page false-merge/split metrics are not yet measured; F04/G5 own that evidence.

## Next

F03 must preserve raw memberships while bounding the fused-container split path in `merge_regions.py`. A contour failure must retain local unresolved candidates rather than granting the detector polygon to the whole component.
