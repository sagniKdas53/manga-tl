# B02: page-scene persistence and upgrade path

Status: **DONE** — the B01 contract now has fresh-install and in-place PostgreSQL storage. B03 may map the immutable records; it must not introduce a legacy reader or conversion path.

Date / app head / worker head / corpus head: 2026-09-14 / `614e2eb841bb1ef4324563d58f6339ebc4a3662e` / `7b54f8c1c516eb445e869b2776ffa62e4ee2762b` / `27de7499f0c4c142763116bfcbcfd0afd9709cbf`.

Allowed files and actual changed files: `database/init.sql`, `database/migrations/20260914_page_scene_v1.sql`, the new idempotent `database/migrate.sh`, its isolated smoke test, production/development Compose migration wiring, and the existing hermetic backend test dependency mount. No source image bytes, project outputs, G0 evidence, A09 labels/reserves, corpus files, or historical records were deleted or converted.

Prerequisites and input artifact hashes: B01 contract at app `614e2eb841bb1ef4324563d58f6339ebc4a3662e`; its scene/asset digests use `page-scene/v1` rules. Existing schema update discovery found that `init.sql` runs only for a new PostgreSQL data directory, while existing installations had no migration runner. This packet supplies the missing one-shot `db-migrate` Compose service and `schema_migrations` ledger.

Current-checkout reproduction (command, observed result): `database/test-page-scene-migration.sh` imported the B01-head dump into an isolated Postgres container, inserted a source `images` row, ran the new migration, and observed the original row remained plus all four new tables. It then imported current `init.sql` into a second fresh database and observed `pages.scene_revision` and all four tables.

GitNexus impact (repo, symbols, callers/processes, risk, freshness): `impact pages --repo manga-library --file database/init.sql --direction upstream --depth 3` returned `Target 'pages' not found`, so the index has no SQL-table symbol/caller graph. A GitNexus execution-flow query confirmed no repository schema-update process beyond fresh `init.sql` application. Storage edits have no indexed direct callers; B03 becomes the first code consumer.

Implementation decision: `pages.scene_revision` starts at zero for existing data. New immutable `page_scene_snapshots` retain the page/revision, only `page-scene/v1`, source digest, logical scene digest and source JSON. `page_scene_owners` persists independent owner/action/override references. `page_scene_assets` retains asset digest, kind, bytes, MIME type and optional storage path. `page_render_jobs` binds a normal job to the immutable snapshot and records input/result/renderer/browser digests and diagnostics. The migration is additive and idempotent; no historical page is backfilled or converted.

Validation commands and executed/pass/fail/not-executed counts:

```text
docker compose -f docker-compose.yml config --quiet
docker compose -f docker-compose.dev.yml config --quiet
sh -n database/migrate.sh database/test-page-scene-migration.sh
PASS: 3/3.

database/test-page-scene-migration.sh
PASS: 1/1 isolated execution; old-schema upgrade preserved the seeded source row and installed 4 tables, and fresh current init installed the same fields/tables.
```

Before/after metrics and artifact paths: before, zero page-scene tables, zero page revision fields and no actual upgrade runner. After, four normalized page-scene tables, one page revision field, one migration, and one `schema_migrations` row per applied file. The test database was destroyed on exit.

Quality-gate result and reviewer: B02 complete; coordinator implementation review recorded here. G1 remains **not executed**: B03, B04 and B05 must prove backend serialization, standalone worker validation and live API round-trip.

Remaining uncertainty or blocker: no backend route creates these rows yet; no worker validates or consumes them; no asset bytes are uploaded in this packet. Existing pages stay at revision zero without a snapshot until a new-format write deliberately creates one.

Exact next command and expected result: inspect `backend-rust/src/models.rs`, its SQL repositories and job queue paths; run GitNexus upstream impact for every existing function/struct that B03 changes; then implement one strict scene mapper and fixture-backed rejection tests.

Files/worktree that must be preserved: B01 contract fixtures/schema/digest rules; B02 migration and migration ledger semantics; frozen G0/A09 evidence and all explicitly unresolved identities.
