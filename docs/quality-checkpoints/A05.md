# A05 - reviewed current baseline issue matrix

Status: `DONE`

Date / app head / worker head / corpus head: 2026-09-10 / `4aa4a9f` / `7d70b64` /
`ed9b191`.

Allowed files and actual changed files: `docs/quality-checkpoints/`,
`docs/quality-runs/a03a04-20260910-retained/`, and the tracker. This task changes
only evidence documentation and an evidence-local ignore rule; no production file or
worker/corpus submodule changed.

Prerequisites and input artifact hashes: A03 and A04 completed in the retained run at
[`a03a04-20260910-retained/`](../quality-runs/a03a04-20260910-retained/). Its
`manifest.json` records source, editor, export, render, and ZIP hashes for all six
fixtures.

Current-checkout reproduction: reviewed each fixture's retained source/original,
editor screenshot, browser export, worker render, project JSON, page snapshot, and
browser-font inventory. All six jobs completed without active, failed, or paused work.

GitNexus impact: not applicable. A05 edits no code symbol or route.

Implementation decision and schema/config/model/font versions: evidence-only. The
captured chapters used local PP-OCRv6, Luna translation/QA, Gemini 3.1 Flash Lite QA,
`useFallbackModels=false`, and 38 loaded browser faces per fixture including Comic
Neue. No model, renderer, or contract decision changed.

Validation commands and executed/pass/fail/not-executed counts:

```text
du -sh docs/quality-runs/a03a04-20260910-retained
PASS: 171 MB retained evidence, primarily third-party image/ZIP payloads.

git diff --check
PASS
```

Before/after metrics and artifact paths: the comparison and issue matrix is
[`A05-comparison.md`](../quality-runs/a03a04-20260910-retained/A05-comparison.md).
All seven reported issue classes remain. No issue is fixed or not reproduced.

Quality-gate result and reviewer: A05 exit gate passed. The coordinator reviewed every
reported defect before scheduling its fix. A06 is ready; M0 remains incomplete pending
labels, synthetic scenes, and the holdout manifest.

Remaining uncertainty or blocker: the raw PNGs and ZIPs intentionally remain untracked
because they contain third-party artwork and comprise 171 MB. The run-local `.gitignore`
allows the small provenance records and report to be committed without adding payloads.

Exact next command and expected result: start A06 with the exact retained source hashes
and draft owner/container/fragment labels for one bounded fixture packet.

Files/worktree that must be preserved: the retained run directory, its manifest and JSON
metadata, plus the ignored local image/ZIP artifacts used for visual review.
