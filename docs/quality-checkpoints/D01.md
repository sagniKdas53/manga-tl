# D01: calibrate SFX classification without authorization

Status: **DONE**

Date / app head / worker head / corpus head: 2026-09-14 / `09e4202427535c2ee5e7c6144afe79100008f9a4` / `5a978680950d9c4519064aa4c943878802613927` / `27de7499f0c4c142763116bfcbcfd0afd9709cbf`.

Allowed files and actual changed files: D01 permits `worker/src/worker/services/layout.py` after reproduction, focused worker tests, and its evidence/checkpoint. Changed `worker/src/worker/services/layout.py`, `worker/tests/test_layout_extra.py`, `docs/quality-runs/d01-20260914-classifier/D01-classifier-evaluation.json`, and this checkpoint. No A09 input/output was read or changed.

Prerequisites and input artifact hashes: B04 is complete. Evaluation consumes the 30 A07 development-only packets and their explicitly recorded final OCR IDs. It joins 257/257 explicit label IDs to retained development OCR/panel records; no UUID/order/proximity/OCR-text identity is inferred. Ten additional A07 labels without a final OCR ID remain unresolved and outside the classifier metric.

Current-checkout reproduction (command, observed result): before the code correction, `PYTHONPATH=src ../.venv/bin/python -c "from worker.services.layout import classify_region_type; print(classify_region_type({'text': '店', 'bboxW': 10, 'bboxH': 10, 'confidence': 0.0}, {'bboxX': 0, 'bboxY': 0, 'bboxW': 1000, 'bboxH': 1000}, 1000, 1000))"` printed `speech`. `confidence = region.get('confidence') or 1.0` discarded valid OCR confidence `0.0`, so the low-confidence sign rule did not execute.

GitNexus impact (repo, symbols, callers/processes, risk, freshness): refreshed the `manga-tl-worker` index at worker `5a97868` (`1,586` nodes, `3,104` edges, `134` flows). Upstream impact for `classify_region_type` is **LOW**: one direct caller, `process_layout`, in two layout processes. No HIGH/CRITICAL finding. The change is tested at the direct caller's classification contract.

Implementation decision and schema/config/model/font versions: preserve confidence zero by defaulting only when the field is absent. The classifier remains descriptive. The coordinator freezes these initial D01 rules for D02–D05:

1. Region type and kana length are evidence, never cleanup or suppression authorization by themselves.
2. Classifier `sfx` is uncertain and becomes source-preserving `review` unless a valid explicit per-region user override selects an action.
3. `preserve`, `review`, and `explain` never enter translation targets/retries/per-region translation QA, automatic cleanup, or automatic replacement-object creation. `explain` is note-only.
4. Only effective `replace` can enter automatic translation, cleanup, or automatic object creation. A valid explicit override wins only for that one region.
5. Missing evidence and every uncertainty resolve to `review`.

Validation commands and executed/pass/fail/not-executed counts:

```text
../.venv/bin/python -m pytest -q tests/test_layout_extra.py
PASS: 11 tests, including the new valid-confidence-zero sign regression.

../.venv/bin/python -m ruff check src/worker/services/layout.py tests/test_layout_extra.py
../.venv/bin/python -m ruff format --check src/worker/services/layout.py tests/test_layout_extra.py
PASS after formatting: 0 lint findings; both files formatted.
```

Before/after metrics and artifact paths: [D01 classifier evaluation](../quality-runs/d01-20260914-classifier/D01-classifier-evaluation.json) replays the current classifier against 257 explicit A07 development labels: replace=172, preserve=53, explain=5, review=27. SFX candidate precision is 8/8 (100%) against explicit preserve/replace labels; recall is 8/53 (15.09%), with 45 saved false negatives and zero saved false-positive replace-dialogue records. It separately records 92 vertical-dialogue and 9 decorated-dialogue replace records with zero SFX classifications. The new regression preserves confidence `0.0` as real evidence.

Quality-gate result and reviewer: D01 task gate is **DONE**. No policy action changed yet; G3 remains unexecuted. The low recall makes automatic SFX suppression unsafe, which is why D02 will turn classifier SFX into `review` absent an explicit override.

Remaining uncertainty or blocker: A classifier cannot infer protected artwork, ownership, or a cleanup boundary. The 37 A07 review decisions and every explicit unresolved identity remain non-destructive. D01 does not calibrate on A09 and does not assert a quality pass.

Exact next command and expected result: implement and test the D02 pure worker policy module; classifier SFX and unknown evidence should produce `review`, while valid explicit override wins only for the specified region.

Files/worktree that must be preserved: frozen A09 roster/reserves/labels, G0 closure evidence, all A06/A06-C/A07 packets, and `docs/quality-runs/d01-20260914-classifier/D01-classifier-evaluation.json`.
