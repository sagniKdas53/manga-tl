# D03: keep policy-skipped OCR out of provider prompts

Status: **DONE**

Date / app head / worker head / corpus head: 2026-09-14 / `09e4202427535c2ee5e7c6144afe79100008f9a4` / `5a978680950d9c4519064aa4c943878802613927` / `27de7499f0c4c142763116bfcbcfd0afd9709cbf`.

Allowed files and actual changed files: D03 permits `worker/src/worker/handlers/translation.py`, focused translation tests, and this checkpoint. Changed that handler and `worker/tests/test_translation_pipeline.py`; added this checkpoint. No provider call, source image, OCR record, A09 artifact, backend schema, or cleanup asset changed.

Prerequisites and input artifact hashes: D02 supplies `select_region_action`. D01's development-only metric/frozen rule set remains at `docs/quality-runs/d01-20260914-classifier/D01-classifier-evaluation.json`; no holdout source, label, reserve, or output is read.

Current-checkout reproduction (command, observed result): before D03, `process_translation` built `page_manifest_entries` by iterating every `ocr_regions` record and sent the full manifest as provider context even when `should_typeset_region` had dropped an SFX target. That repeated raw skipped SFX in a paid prompt.

GitNexus impact (repo, symbols, callers/processes, risk, freshness): refreshed the worker graph at the D02 state (`1,614` nodes, `3,144` edges, `137` flows). Upstream impact for `process_translation` is **LOW** with zero graph-resolved direct callers/processes; focused pipeline tests exercise the queue callback boundary. No HIGH/CRITICAL finding.

Implementation decision and schema/config/model/font versions: policy selection runs before all target/quality decisions. Any action other than effective `replace` is recorded in local policy metadata and omitted from chunks, `translate_batch_llm`, retries, fallback, and the provider prompt's repeated target manifest. The callback sends only selected target translations; this avoids legacy backend blank `LayerElement` creation for a source-preserving skipped record. It records selected IDs, skipped IDs/actions/reasons/uncertainty, and actual planned chunk count in a `policy` metadata object. OCR records remain backend data; no skipped raw text is injected into a provider prompt. Existing quality-filtered replacement targets remain filtered separately from policy skips.

Validation commands and executed/pass/fail/not-executed counts:

```text
../.venv/bin/python -m pytest -q tests/test_translation_pipeline.py tests/test_translation_service.py tests/test_region_policy.py
PASS: 32 tests. New D03 coverage uses one classifier-SFX review and one explicit replace dialogue: provider input, repeated manifest, callback translation IDs, selected/skipped metadata, and chunk count contain only the dialogue.

../.venv/bin/python -m ruff check src/worker/handlers/translation.py tests/test_translation_pipeline.py
../.venv/bin/python -m ruff format --check src/worker/handlers/translation.py tests/test_translation_pipeline.py
PASS: zero lint findings; both files formatted.
```

Before/after metrics and artifact paths: D03's focused trace has two OCR records, one policy review skip and one explicit replace target: provider target IDs 1/2, repeated raw skipped entries 0, selected chunks 1, and no assumed token count. The test mocks the provider, so actual provider token/cost fields are absent by design; live per-run cost remains the existing `cost` payload only when a real provider returns usage.

Quality-gate result and reviewer: D03 task gate is **DONE** at the target-selection/repeated-manifest boundary. G3 remains unexecuted pending retry and translation-QA scheduling plus contract-level authorization.

Remaining uncertainty or blocker: page-wide visual QA deliberately remains outside this filter and may inspect source SFX. Per-region translation QA scheduling is D04. New-format cleanup/object creation is D05; no D03 callback is treated as that authorization.

Exact next command and expected result: inspect and update the actual per-region QA payload builders. A policy-skipped region must be absent from per-region translation QA while page-wide visual QA still retains the source image.

Files/worktree that must be preserved: D01 evaluation, D02 policy surface, frozen A09 artifacts, and all immutable A06/A06-C/A07 evidence.
