# B01: freeze the new page-scene contract

Status: **DONE** — B01 is frozen for B02–B05 consumers. G1 remains pending their independent storage/backend/worker/API evidence.

Date / app head / worker head / corpus head: 2026-09-14 / `7df219949c5eb1bc3beb809b90e1d2a50f728de2` / `7b54f8c1c516eb445e869b2776ffa62e4ee2762b` / `27de7499f0c4c142763116bfcbcfd0afd9709cbf`.

Allowed files and actual changed files: B01 allows `contracts/` plus its task checkpoint. Added `contracts/page-scene-v1.schema.json`, `contracts/page-scene-v1.md`, `contracts/validate-page-scene-v1.cjs`, and the fixture suite under `contracts/fixtures/page-scene-v1/`; added this checkpoint. No production source, API, database, worker, frontend, corpus, G0 evidence, A09 roster, reserve, label, or historical artifact changed.

Prerequisites and input artifact hashes: G0 is passed at `docs/quality-runs/g0-20260913-closure/`. B01 consumes only the recorded M1 contract requirements; it does not use or reinterpret the 267 explicitly unresolved fresh source-owner/action identities, and it does not score A09.

Current-checkout reproduction (command, observed result): `read contracts` returned `Path 'contracts' not found`. This checkout had no page-scene contract to preserve. The new schema deliberately accepts only `contract_version: "page-scene/v1"`; the unsupported-version fixture is rejected.

GitNexus impact (repo, symbols, callers/processes, risk, freshness): no existing function, class, method, API or worker symbol changed, so no symbol has an upstream blast radius. The required GitNexus context resource was unavailable from this environment (`No MCP server has resource ...`); the failed lookup was reported through `xd://report_issue`. New contract artifacts have no callers until B03–B05.

Implementation decision and schema/config/model/font versions: `contracts/page-scene-v1.schema.json` is a strict new-format-only schema. `contracts/page-scene-v1.md` fixes logical versus resolved scene semantics, independent owners, effective policy action, cleanup/source/asset identity, explicit manual cleanup, finite fractional source geometry, JCS/SHA-256 digest rules, renderer job/result identity and new-archive rejection rules. The fixture runner validates structural and cross-record semantics, including the resolved logical-scene digest. No model, font, renderer, browser, database or provider configuration changed.

Validation commands and executed/pass/fail/not-executed counts:

```text
node contracts/validate-page-scene-v1.cjs
PASS: 3 valid fixtures; 9 invalid fixtures rejected.

Fresh temporary copy: cp -a contracts <temporary>/contracts; AJV_MODULE=<frontend node_modules Ajv> node <temporary>/contracts/validate-page-scene-v1.cjs
PASS: the same 3 valid / 9 invalid results. The copied contract reads no old project, schema, DB, source image, archive or compatibility path.
```

Before/after metrics and artifact paths: before, no `contracts/` directory existed. After, the versioned schema/document/runner plus 3 valid and 9 invalid fixture cases are at `contracts/`. Valid coverage includes 12.5° and fractional coordinates, a blank manual text object, an explicit manual-cleanup object, confidence `0`, preserved SFX without cleanup/object, and a joint active-set cleanup artifact for overlapping replacement owners. Invalid coverage rejects legacy version, non-finite JSON, invalid geometry type, empty automatic text, preserve-with-cleanup, missing cleanup asset, wrong cleanup source, logical/resolved mixing, and mismatched logical digest.

Quality-gate result and reviewer: B01 complete; coordinator freeze is this checkpoint. G1 is **not executed**: B02–B05 must prove fresh/upgrade storage, backend serialization, standalone worker validation, and live API/generated-type round-trip against these fixtures.

Remaining uncertainty or blocker: B01 intentionally does not supply a database migration, backend DTO, worker package copy, API endpoint, renderer, or archive implementation. Those consumers must include an identical versioned schema or generated model with a digest check; they must not use a parent-path symlink or add a legacy reader.

Exact next command and expected result: start B02 by inspecting `database/init.sql` and the actual upgrade path, run required GitNexus impact before each existing symbol edit, then prove a fresh isolated DB and existing-schema upgrade without source deletion.

Files/worktree that must be preserved: all `contracts/page-scene-v1*` artifacts and fixtures; G0 closure evidence; frozen A09 manifest/labels/reserves; immutable corpus sources and historical evidence.
