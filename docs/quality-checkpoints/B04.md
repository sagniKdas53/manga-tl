# B04: standalone worker page-scene validation

Status: **DONE** — worker commit `6532dd9` packages a strict new-format `page-scene/v1` validator and Pydantic job envelope without importing the parent checkout or using a symlink.

Date / app head / worker head / corpus head: 2026-09-14 / `cb3f9de` / `6532dd9` / `27de7499f0c4c142763116bfcbcfd0afd9709cbf`.

Allowed files and actual changed files: worker `src/worker/page_scene.py`, `src/worker/schemas.py`, and `tests/test_page_scene_contract.py`; this parent checkpoint/tracker and the submodule pointer. No worker pipeline handler, renderer, source asset, historical project, G0 evidence, A09 label/reserve or corpus file changed.

Prerequisites and input artifact hashes: B01 contract at `614e2eb`; B02 storage at `473deb2`; B03 mapper at `cb3f9de`. The worker artifact module declares `CONTRACT_VERSION = "page-scene/v1"` locally and contains all validation/digest behavior it needs at runtime.

GitNexus impact: `JobData` upstream impact in `manga-tl-worker` found one direct importer (`src/worker/main.py`), zero affected processes and **LOW** risk. B04 adds a separate `PageSceneRenderRequest`, leaving `JobData` and its importer unchanged. The module-file target is not indexed as a symbol; no high/critical risk occurred.

Implementation decision: `PageSceneRenderRequest` requires the versioned job envelope and calls the standalone artifact validator. The validator rejects legacy versions, non-finite geometry, invalid logical/resolved boundary, missing/duplicate owner identities, unauthorized cleanup, empty automatic text, missing artifact references, source mismatch and resolved digest mismatch. It returns immutable document/digest identity; B05 owns queue/API wiring.

Validation:

```text
../.venv/bin/python -m pytest -q tests/test_page_scene_contract.py
PASS: 2/2 tests; all 3 shared valid scenes accepted, non-finite JSON and all 9 shared invalid cases rejected.

../.venv/bin/python -m ruff check src/worker/page_scene.py src/worker/schemas.py tests/test_page_scene_contract.py
PASS.

Copied worker-only directory + copied fixture corpus, PAGE_SCENE_FIXTURES=<temporary fixtures> pytest -q tests/test_page_scene_contract.py
PASS: 2/2. No parent import or symlink was present.
```

Quality-gate result: B04 complete. G1 remains **not executed**: B05 must implement the live backend API, generate frontend types, persist/read the strict records and prove the round trip.

Exact next command: B05 must first impact the selected backend routes, then add a bounded scene endpoint/mapping and run `npm run generate-api` against the live backend before browser/API verification.
