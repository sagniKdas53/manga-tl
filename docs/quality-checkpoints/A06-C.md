# A06-C: conventional-control baselines and owner labels
Status: ACTIVE

Date / app head / worker head / corpus head: 2026-09-11 / `62f4f93f9c5be1d979d2c263244d964d27def23c` / `7d70b64baca51690abf4dfbd7a89c2cde56b2c7e` / `ed9b191da255e4d3d132a2d050ab8fffa1815e26`.

The baseline runner accepts explicit control fixtures while retaining its six-fixture default. A06-C evidence includes the two isolated retained runs, source-pixel review sheets, and reviewed label sets. A later, narrowly scoped worker eligibility guard is recorded below; no frontend, backend, corpus source, model, schema, or submodule changed.

Prerequisites and input artifact hashes: A06-C0's resolved selection pins `sample7` to `corpus/samples/ja/sample7/source.jpeg`, SHA-256 `7e23ae0c9e7ffbec45221c2820a35dcde2e81aae99d34eb922d71f25855f71e9`, 1299x1831, active JA, `bw_panel_manga`. The local digest matched both the selection and the newly saved manifest.

Current-checkout reproduction (command, observed result): the first bounded invocation rejected `sample7` before uploading it because the historical fixed runner did not list conventional controls: `QUALITY BASELINE FAILED: unknown fixed fixture: sample7`. The runner was then limitedly extended and rerun with `node scripts/playwright/capture_quality_baseline.cjs --base http://localhost:18081/tlhub --out docs/quality-runs/a06-c-20260910-sample7 --register --fixture sample7`. A new `manga-quality-a06c-sample7` stack on loopback port 18081 completed OCR, translation, QA, render and browser exports in 108.185 seconds. The database and all named volumes are isolated from the normal dev stack and remain running for this retained run.

GitNexus impact (repo, symbols, callers/processes, risk, freshness): attempted upstream impact/context for `main` in `scripts/playwright/capture_quality_baseline.cjs`; this standalone runner is not indexed, so GitNexus returned `UNKNOWN` risk with no discovered caller or process. The file was inspected directly: it is an opt-in local capture utility and has no production import path. No HIGH or CRITICAL result occurred.

Implementation decision and schema/config/model/font versions: the runner retains its six historical default fixtures. `--fixture` selects one explicitly listed fixture; it rejects an unknown name. This packet adds only `sample7` to the separate conventional-control list. The isolated run used local `PaddleOCR(PP-OCRv6_medium_rec)` from the OCR Worker and `openrouter` `openai/gpt-5.6-luna` translation. The retained page snapshot reports 38 loaded browser font faces. Runtime: Linux 5.15.0-181-generic x86_64, Intel Core i5-7200U (4 CPUs); GPU inventory reports NVIDIA GeForce 940MX driver 580.159.03. Backend image `sha256:f3cfe0573fc7a240cbadc5e027754a5cfb40bc1a90673ff8d413f28d79652141`; worker image `sha256:2aed14b20244ed299d9da606fbcdce5b4b57af049b4d04ca131f1969ed2f2b24`.

Validation commands and executed/pass/fail/not-executed counts:

```text
node --check scripts/playwright/capture_quality_baseline.cjs
node scripts/playwright/capture_quality_baseline.cjs --help
PASS: 2 / FAIL: 0 / NOT-EXECUTED: 0

docker compose -f docker-compose.dev.yml config --quiet
docker compose -f docker-compose.dev.yml ps
curl --fail http://localhost:18081/tlhub/actuator/health
./.venv/bin/python scripts/dev_setup.py models --languages ja
PASS: isolated config, DB/Redis/MinIO/backend/worker health, endpoint, and JA model warmup / FAIL: 0 / NOT-EXECUTED: 0

node scripts/playwright/capture_quality_baseline.cjs ... --fixture sample7
PASS: one fresh pipeline and A04 export capture / FAIL: 0 / NOT-EXECUTED: 0

jq owner-label schema/count and exact final-OCR-ID set comparison
sha256sum source and browser artifacts
unzip -t project.zip
git diff --check
PASS: label accounting, source identity, four export hashes, ZIP integrity, and diff whitespace / FAIL: 0 / NOT-EXECUTED: 0
```

Before/after metrics and artifact paths: `a04-exports/sample7/` retains `editor.png` SHA-256 `610bb1cddecc00a257960f224506f261c449916ed2d892c59d438a36d9f4cd09`, `export.png` `0597dce7c1956e3ed521a60fe8cc6f74625a5e38559ba9fa9ea90292d5215ffc`, `rendered.png` `40b880a0ef1fd80ef4771813053e21bab403b3fbadfe6c783db8f84a98b05b3f`, and `project.zip` `032d65a29d4ef74dd4ce33093fba7930ba7e40920147cb7bae658638acd4282e`. Its page snapshot has 3 detected panels, 24 final OCR regions (9 speech, 1 SFX, 14 captions), 15 `passed` and 9 `reject_sfx` QA statuses. The coordinator-reviewed `A06-C-owner-labels.sample7.draft.json` accounts for every final region: 17 locally bounded owner drafts and 7 unresolved free-standing/decorative records.

Quality-gate result and reviewer: coordinator reviewed the immutable source and fresh editor capture. The ordinary white balloon interiors, outlines/tails, gutters, adjacent art, and local panel ownership were explicitly inspected. Shared `bubble_7`, conversation ordering, and proximity were not treated as ownership evidence. A06-C remains ACTIVE: one of 24 source hashes has fresh provenance and reviewed draft labels; G0, A07, A09, and all milestones remain pending.

Remaining uncertainty or blocker: no raw A02 oriented fragments/grouping edges are retained by this runner, so these labels cannot verify source-fragment membership. Seven decorative/free-standing records deliberately remain unresolved. Current output demonstrates translated text and broad text boxes across ordinary controls; it is evidence, not an algorithm fix or a passed visual-quality gate.

Exact next command and expected result: select the next control as a new bounded A06-C packet, add only that source to the runner's explicit conventional-control list after repeating impact inspection, then run `node scripts/playwright/capture_quality_baseline.cjs --base http://localhost:18081/tlhub --out <new-run-directory> --register --fixture <selected-sample>` against an isolated stack. It must create fresh stage and browser/export artifacts without changing historical runs or corpus sources.

Files/worktree that must be preserved: `docs/quality-runs/a06-c-20260910-sample7/`, its running isolated Compose project/volumes, the immutable `sample7` source, the resolved selection, A01-A06 retained evidence, `scripts/playwright/capture_quality_baseline.cjs`, and this checkpoint.

## 2026-09-11 continuation: remaining 23 controls

The historical runner accepted registration only for the first user on a fresh isolated database. A second process therefore failed before upload with HTTP 400. The runner now allows repeated `--fixture` flags and retains one authenticated process, while creating separate source-language-correct series (`ja`, `ko`, `zh`). It still processes each fixture serially and retains each page's own chapter, snapshot, editor PNG, exported PNG, rendered PNG, ZIP, unpacked project, and browser-font inventory.

A second isolated stack, `manga-quality-a06c-controls` on port 18082, warmed JA/KO/ZH local OCR and completed the remaining 23 selected sources. Together with sample7, the two manifests account for 24 unique controls: 8 JA, 8 KO, and 8 ZH. All 23 new pages have four retained export hashes; the control manifest carries 204 final OCR regions. `A06-C-owner-labels.controls.draft.json` gives every one of those regions an independent conservative draft owner with no inferred shared container or membership. Node syntax checks, JSON validation, source-digest validation, manifest/export-hash checks, service health, and `git diff --check` passed.

This did not initially pass A06-C or make A07 ready. The 204 labels were generated drafts, not a coordinator's per-region visual owner/container review. They deliberately avoided false merges, but their unresolved containers and absent raw A02 fragments left the required reviewed ownership evidence incomplete.

## 2026-09-11 source-pixel review

`scripts/generate_a06c_review_sheets.py` generated retained source-pixel overview and numbered crop sheets under each run's `a06-c-review-sheets/`. `scripts/finalize_a06c_control_review.cjs` then recorded only the visual conclusion for each final OCR box; it does not assert unavailable raw-fragment memberships or change the pipeline.

The reviewed sets are `a06-c-20260910-sample7/A06-C-owner-labels.sample7.reviewed.json` and `a06-c-20260911-controls/A06-C-owner-labels.controls.reviewed.json`. `sample7` resolves all 24 final OCR boxes: 13 independent dialogue owners, 9 non-dialogue elements, and 2 free-standing captions. The remaining controls resolve 202 of 204 boxes: 156 independent dialogue owners, 37 non-dialogue elements, and 9 free-standing captions. All source hashes still match the selection/manifests; representative rechecks include sample7 `7e23ae0c9e7ffbec45221c2820a35dcde2e81aae99d34eb922d71f25855f71e9`, sample134 `77f11ee385bd82fd183c1856c9a6308c01e2dfb6ee7b806d6e7d041c5f4a407f`, and sample261 `ff8c69ff142d3d09f7473c3e2a4d532ec6d5a0d25eaff99e44dfde2c2aec14d3`.

The reviewer explicitly checked ordinary white-bubble interiors, borders/tails, gutters, adjacent art, non-dialogue/SFX, and local ownership. The fresh output still preserves rendering failures, including broad replacement text/white areas over `sample47` action/SFX art; QA status is not a quality-gate pass.

## 2026-09-11 live fragment capture for prior fusions

`process_ocr` now has an unset-by-default `OCR_CAPTURE_DIR` observer. When enabled, it writes raw oriented source-space quads, recognition, live detector polygons, the exact per-call grouping components, observed edges, and stable owner IDs after the existing OCR/grouping work completes. The observer catches its own failures and does not alter the callback. `capture_observed_ocr_grouping` records the already-observed groups without rerunning grouping; its focused test, the OCR/grouping test set, Ruff, Pyright, and the complete worker suite passed (`494 passed`). GitNexus pre-change impact for `process_ocr` was LOW with no direct indexed callers; post-change analysis marks the four OCR flows MEDIUM because the entry point changed.

A new isolated `manga-quality-a06c-fragments` stack at `http://localhost:18083/tlhub` rebuilt the worker and processed only sample134 and sample261. The manifest is `docs/quality-runs/a06-c-20260911-fragment-capture/manifest.json`; live captures are `a02-captures/ee5fd698-b471-4193-ba8a-b949463526cf.json` (sample134, SHA-256 `e8ea5bc26f5d7f6534cfda7ac47c0c28652f522d2c175b84aef0649d90f36a1d`) and `a02-captures/32ec54a7-e7eb-4656-890e-f34b02bc9041.json` (sample261, SHA-256 `e312fbefd980260146ea09d8d06599d766cde741a553a14487309654fae8783b`). Both capture paths are `live`, and each has equal raw-quad, recognition, and normalized-region counts.

The capture resolves `sample134-r001`: its observed owner `owner-bed6c586467c606bb040bad1` comprises an empty detector fragment and the SFX `ひーす`, so it is reviewed non-dialogue rather than a source-owner fusion. It also confirms `sample261-r008`: observed owner `owner-b15bcd857dfb0e2998741199` joins six fragments that visibly belong to the same central speech balloon. Its grouping and reading order are correct; the exact fragment membership is retained in the reviewed label set. All 228 final OCR records now have reviewed source ownership/classification.

A06-C remains ACTIVE and A07 must not start: the tracker requires failures as well as unresolved owners to remain blocking. The remaining blockers are baseline rendering failures such as broad replacement text/white areas over `sample47` action/SFX art, not a sample261 grouping or reading-order defect.

## 2026-09-11 sample47 direct-text regression attempt

`sample47` has false direct OCR regions without detected speech containers: page/artifact `0`, SFX-like `大GG`, SFX `2`, and SFX `5 DO`. A narrow `should_typeset_region` guard now permits unenclosed source-script dialogue only (JA/KO/ZH scripts and punctuation); it prevents bare digits, Latin fragments, and mixed-script OCR noise from creating automatic cleanup/text objects. `test_unenclosed_non_source_script_marks_are_not_typeset` covers `0`, `大GG`, and a valid uncontained Japanese dialogue string. The focused translation, OCR/grouping, Ruff, and Pyright checks passed.

Fresh evidence is retained at `docs/quality-runs/a06-c-20260911-sample47-r3/` from the independent `manga-quality-a06c-s47-r3` stack. The new render removes the former top-right `0`, mid-left `How’s that?`, and lower-left `Hurry...` false-positive replacement plates. It does **not** pass the preservation requirement: the central grey `Not bad.` replacement remains over source SFX even though its translation-layer element for OCR `大GG` is empty and `visible: false` in `page-snapshot.json`. The upper-left bubble OCR (`チョイスじないかなかなか渋いストーン？パエジナ`) was translated plausibly but was not source-corrected; no unverified Japanese transcription was inserted. Both isolated stacks (`manga-quality-a06c-fragments` and `manga-quality-a06c-s47-r3`) and all of their named volumes were removed after capture; retained artifacts are committed under `docs/quality-runs/`.

A06-C remains ACTIVE. The next bounded investigation must trace why the rendered artifact retains a hidden, empty `大GG` replacement and must verify the upper-left source text before applying an OCR correction. A07 remains blocked.
