# A03 — fresh six-page current pipeline baselines

Status: `BLOCKED`

Date / heads: 2026-09-09; app `9bf0a2a`; worker `4b56c48`; corpus `ed9b191`.

The exact six current source lanes were inventoried and their hashes, dimensions, runtime
provenance, and per-stage outcomes are saved in the new run directory
[`docs/quality-runs/a03-20260909/manifest.json`](../quality-runs/a03-20260909/manifest.json).

The isolated A01 service preflight passed and is recorded at
[`A03-preflight.json`](A03-preflight.json). It verifies disposable Postgres, Valkey, and MinIO,
but does not provide a serving backend or worker. The normal Compose stack was not running;
`http://localhost:8080/tlhub/actuator/health` returned HTTP `000`, and no manga-library backend
or worker container was present. The six sources are Japanese and are present under
`corpus/samples/ja/`.

No current OCR, translation, cleanup, or render call was made. Existing OCR/translation benchmark
tools require their own corpus region/reference layouts and do not map these six source pages to a
fresh production run. Existing `project/`, `render.png`, and `export.png` files are historical and
were not copied or replayed. Consequently no stage is labelled `live` or `cached`; each is
explicitly `stubbed`/`not-executed` in the manifest. No algorithms or production symbols were
edited. The worker working tree already contains the A02 capture files and was preserved.

Validation performed:

```text
.venv/bin/python scripts/quality_preflight.py --report docs/quality-checkpoints/A03-preflight.json
  PASS (isolated DB sentinel, Redis, and storage verified)
.venv/bin/python scripts/benchmark_vlm_ocr.py --help
  PASS (tool inventory only; no page call)
.venv/bin/python scripts/benchmark_translation.py --help
  PASS (tool inventory only; no page call)
```

The G0 gate remains open. Resume by starting the current backend/worker stack with credentials and
models available, then run one exact source page through OCR, translation, cleanup, and render,
writing each stage artifact and call record into this run directory before proceeding to the next
page. Do not use the historical project layout as a source-region mapping.
