# A03 — fresh six-page current pipeline baselines

Status: `DONE`

## Superseding retained run

The earlier run below completed its pipeline but deleted five chapters before A04 could consume
them. The replacement isolated project `manga-quality-a03r` uses loopback port `18081` and separate
volumes, leaving the existing dev stack untouched. Its retained evidence is
[`a03a04-20260910-retained/`](../quality-runs/a03a04-20260910-retained/).

App/worker/corpus heads: `4aa4a9f` / `7d70b64` / `ed9b191`. All six chapters pin local
`PP-OCRv6`, Luna translation and QA LLM, Gemini 3.1 Flash Lite QA VLM, and
`useFallbackModels=false`; the isolated database query verified those settings on every chapter.
Every pipeline reached an empty active-job list with no failed or paused jobs. The manifest records
source hashes, page snapshots, and A04 screenshot/export/render/ZIP hashes. It recorded 15 Luna
and 8 Gemini calls, totaling 90,002 prompt tokens, 30,279 completion tokens, and `$0.067886`.

Validation: `node --check scripts/playwright/capture_quality_baseline.cjs`; runner help; isolated
Compose health; six required A04 artifact sets present. GitNexus found the new standalone runner
absent from the index before creation: zero callers/processes, `UNKNOWN` risk. No production symbol
changed. Preserve this stack and run directory for A05.

Date / heads: 2026-09-10; app `b77a912`; worker `7d70b64`; corpus `ed9b191`.

All prior A03 runtime data and uncommitted run artifacts were deleted. The isolated Compose project
was removed with all its volumes (database, object store, queue, model cache, and render cache) and
recreated. The first user on the replacement database is an API-verified administrator, so each
disposable series can be deleted through the product API.

The clean run directory is
[`docs/quality-runs/a03-20260910-fresh-admin/`](../quality-runs/a03-20260910-fresh-admin/).
It records configuration, live API responses, source and render digests, and sanitized job payloads.
No historical page, OCR, translation, or render output was reused.

All six clean lanes completed. Each used local PP-OCRv6, OpenRouter GPT-5.6 Luna for translation
and QA LLM, Gemini 3.1 Flash Lite for QA VLM, and `useFallbackModels=false`. The administrator
deleted the preceding disposable series through the product API before creating the next series and
chapter.

```text
sample177 — source 8c610bdc…e28f382
series  63bffaa4-d2e6-40c2-a051-4af180d84ff2
chapter faf78cea-4533-4a75-8431-dbc2f4cc2edc
page    23cefeac-a7fa-4b90-8539-c2dd101f8ed4

OCR         local / PP-OCRv6
translation openrouter / openai/gpt-5.6-luna
QA LLM      openrouter / openai/gpt-5.6-luna
QA VLM      openrouter / google/gemini-3.1-flash-lite
fallback    disabled

panel detection, OCR, layout, translation, QA, and render: COMPLETED
OCR regions: 4; final QA statuses: passed; render: 2048x1910
```

The run contains 79 OCR regions in total. Final QA outcomes are retained rather than normalized:
`sample177` passed, `sample222` needs manual review, `sample61`, `sample99`, and `sample93` include
both `passed` and `reject_sfx`, and `sample83` has no QA verdict. All completed job records,
resolved chapter configurations, source hashes, and render hashes are listed in
[`manifest.json`](../quality-runs/a03-20260910-fresh-admin/manifest.json).

Validation performed:

```text
docker compose -f docker-compose.dev.yml ps
  PASS (backend healthy; isolated dependencies running)
POST /tlhub/api/auth/register
GET  /tlhub/api/auth/me
  PASS (fresh first user is role=admin)
python3 scripts/dev_setup.py models --languages ja
  PASS (PP-OCRv6 Japanese detector and recognizer warmed)
GET /tlhub/api/jobs
  PASS (all six fixture queues drained; every recorded job completed)
```
