# G1: page-scene/v1 contract gate

Status: **PASSED** — B01 through B05 now provide the frozen new-format-only contract, additive persistence, strict backend mapping, standalone worker validator/digest pin, and live browser-facing API/generated-TypeScript round trip.

Date / implementation commits: 2026-09-14 / B01 `614e2eb`, B02 `473deb2`, B03 `cb3f9de`, B04 worker `5a97868` (parent `0e5c407`), B05 `701dd6d` and `963abf0`.

## Contract evidence

- B01 supplies the authoritative schema, canonical logical digest, 3 valid fixtures, and 9 invalid cases. It permits finite fractional source geometry, signed 12.5° rotation, blank manual text, manual cleanup, confidence `0`, preserved SFX, and overlapping replacement cleanup. It rejects unsupported versions, malformed/non-finite geometry, unauthorized preserve cleanup, missing assets, wrong source, logical/resolved mixing, and wrong resolved logical digest.
- B02 persists only `page-scene/v1` snapshots plus normalized owner/action/override and asset identity rows, without converting historical projects.
- B03 validates and maps the backend document before the database boundary.
- B04 packages the worker validator independently, with a local authoritative-schema SHA-256 pin and no parent checkout symlink/import.
- B05 binds a scene to the live route page UUID, current revision, and actual source SHA-256 before atomic snapshot/owner/asset persistence. It accepts only logical writes; resolved output cannot become editable source of truth.

## Executed cross-runtime checks

```text
node contracts/validate-page-scene-v1.cjs
PASS: 3 valid fixtures accepted; non-finite JSON plus 8 mutation cases (9 invalid inputs total) rejected.

backend-rust/cargo test --test page_scene_contract
PASS: 2/2 tests; all 3 shared valid documents round-trip through the Rust validator and all shared invalid inputs reject.

worker/../.venv/bin/python -m pytest -q tests/test_page_scene_contract.py
PASS: 3/3; worker schema digest matches the authoritative copy, 3 shared valid documents accept, and all shared invalid inputs reject.

backend-rust/./scripts/test-env.sh run cargo test --test pages_endpoints page_scene_api_round_trips_the_new_format_contract
PASS: 1/1 isolated live API test. Source/page/revision identity is bound before persistence, owner/asset rows persist, 12.5° is retained, same-revision divergence is 409, and the legacy version is 400.

Actual Chromium browser request against the isolated backend:
PASS: a logical fixture remapped only to the live UUID/source SHA-256 wrote with 200, read with 200, retained source identity and 12.5° exactly, and the same legacy-version mutation returned 400.

frontend npm test -- src/__tests__/api/pageScene.test.ts && npm run typecheck
PASS: 2/2 generated-type adapter tests and the full TypeScript type check.
```

## Gate decision

G1 requirements are met: backend, worker, and browser-facing live API paths agree on valid/invalid contract behavior; finite fractional geometry is retained with zero observed angle error; policy authorization and source/asset identities are validated; and only the new-format round trip is supported. No historical-format support matrix, converter, fallback renderer, or legacy default was added.

M1 is complete. C01, D01, E01, and F01 may start under their own packets and impact analyses; no downstream task is implied complete by this gate.