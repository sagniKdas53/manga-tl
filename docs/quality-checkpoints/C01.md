# C01: page freshness after element edit

Status: **DONE** — B05 and G1 are complete; the real API/DB wrong-ID path is reproduced, corrected at its incorrect caller and covered by an isolated-service regression.

Date / base app head / worker head / corpus head: 2026-09-14 / `26bbd8d` / `5a97868` / `27de7499f0c4c142763116bfcbcfd0afd9709cbf`. C01 changes are bounded to the app route, its isolated regression test and the tracker/checkpoint record.

## Frozen packet

- **Allowed production file:** `backend-rust/src/routes/layers.rs`.
- **Allowed test file:** `backend-rust/tests/layers_endpoints.rs`.
- **Expected behavior:** editing element X updates only X's owning page; a different layer/page never receives the freshness timestamp.
- **Focused gate:** `./scripts/test-env.sh run cargo test --test layers_endpoints <new-C01-test>` from `backend-rust/`. The isolated PostgreSQL/Valkey/MinIO stack must become healthy; a skipped test is not evidence.
- **Non-goals:** no `scene_revision` increment, queue scheduling, render callback, generalized `touch_page` refactor, OpenAPI change, or legacy behavior work. C02 owns transactional page-revision increments and all other render-affecting mutation inventory.

## Reproduction

An isolated PostgreSQL/Valkey/MinIO stack and backend were started. The probe created a series, chapter, uploaded page, translation layer, and text element through the real HTTP API. After resetting that page's `last_edited_at` to `NULL`, it sent:

```text
PUT /tlhub/api/layer-elements/c325474d-d3f8-44d9-9a32-791d298f0e43
{"text":"after"}
→ 200, updatedText="after"

SELECT COALESCE(last_edited_at::text, 'NULL') FROM pages
WHERE id = '93a42c9e-f33c-4cd7-a3cc-2b9610552388'
→ NULL
```

The route therefore reports a successful element mutation while leaving its owning page ineligible for the existing timestamp-based render scan.

## Root cause and safe change

`update_layer_element` receives an **element** UUID, then calls `touch_page(&state.pool, id)`. `touch_page` expects a **layer** UUID and resolves the page through `layers.id`; no matching layer exists for an ordinary element UUID, so the update affects zero page rows. The handler already has the correct `updated.layer_id` from `RETURNING *`.

GitNexus upstream impact:

- `update_layer_element`: **LOW**, zero direct callers/processes.
- `touch_page`: **CRITICAL**, six direct callers across six flows (`create_page_layer`, `create_image_layer`, `delete_layer`, `update_layer`, `create_layer_element`, and `update_layer_element`).

The change must be only `touch_page(&state.pool, updated.layer_id)`. Do not change `touch_page` itself.

The regression test must seed page A/layer A/element X plus a distinct page B/layer B whose layer UUID deliberately equals X's element UUID (separate tables permit this). Before the edit, reset both timestamps. The current bug marks B; the corrected handler marks only A. This proves both the positive owner relation and the wrong-ID exclusion instead of merely asserting that some page timestamp changed.

The bounded production and regression-test changes are now complete; no broader lifecycle work is in this packet.

## Completed change

`update_layer_element` now calls `touch_page(&state.pool, updated.layer_id)`. The handler's
`RETURNING *` row already supplies this owning layer UUID; `touch_page` still resolves the page
through `layers.id` unchanged. No page revision, queue, callback, API or general `touch_page`
behavior changed.

## Focused validation

```text
backend-rust$ ./scripts/test-env.sh run cargo test --test layers_endpoints element_update_touches_only_its_owning_page
...
test element_update_touches_only_its_owning_page ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 3 filtered out
```

The command created healthy isolated PostgreSQL, Valkey and MinIO containers; the test did not
take its absent-environment early return. It creates page A/layer A/element X and page B/layer B
where `layer B.id == element X.id`, resets both timestamps, then mutates X through the HTTP route.
Page A becomes fresh and page B remains `NULL`, proving both the owner update and wrong-ID
exclusion.