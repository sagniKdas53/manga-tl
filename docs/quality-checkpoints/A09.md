# A09: independent language/style/source-family holdout
Status: BLOCKED

Date / app head / worker head / corpus head: 2026-09-11 / `e7b811040f6b37bc28a9b4a1f8fbf995f2bacca4` / `7b54f8c1c516eb445e869b2776ffa62e4ee2762b` / `ed9b191da255e4d3d132a2d050ab8fffa1815e26`.

Allowed files and actual changed files: `docs/quality-runs/a09-20260911-holdout/A09-holdout-inventory.json`, this checkpoint, and the tracker status update. No corpus source, source metadata, image, reference, application, worker, backend, schema, model, or submodule changed.

Prerequisites and input artifact hashes: A07 and A08 are complete. The durable [A09 inventory](../quality-runs/a09-20260911-holdout/A09-holdout-inventory.json) is pinned to current heads and candidate source SHA-256 values. The source inventory decodes 637 corpus entries with 636 distinct hashes; `sample258` / `sample637` are the sole exact duplicate pair.

Current-checkout reproduction (command, observed result): `./.venv/bin/python docs/quality-evidence/audit_style_coverage.py --output /tmp/opencode/a09-style-coverage-current --selection docs/quality-evidence/style-coverage-20260910/resolved-selection.json` completed with 637 decoded sources and zero decoding errors. Direct enlarged-sheet review and individual source review found only `sample201` and `sample205` as candidate ZH pages outside the development set that are Chinese-dialogue, multi-panel, conventional-balloon pages. Four are required.

GitNexus impact (repo, symbols, callers/processes, risk, freshness): not applicable. A09 is an evidence-only inventory and changes no production symbol, API, worker flow, route, source, or submodule. No implementation impact is implied by the recorded source shortfall.

Implementation decision and schema/config/model/font versions: no holdout manifest, region labels, threshold set, model, or renderer is frozen. The retained inventory is `a09-holdout-inventory/v1`, a blocked research artifact. It holds 8 provisional JA and 8 provisional KO rows plus the two qualifying ZH rows, not an evaluable 24-page set.

Validation commands and executed/pass/fail/not-executed counts:

```text
./.venv/bin/python docs/quality-evidence/audit_style_coverage.py --output /tmp/opencode/a09-style-coverage-current --selection docs/quality-evidence/style-coverage-20260910/resolved-selection.json
PASS: 637 decoded sources, 0 source errors, 636 unique bytes, 24 development controls preserved.

jq -e '.status == "blocked" and (.shortlist.zh | length == 2) and (.blockers | length == 3)' docs/quality-runs/a09-20260911-holdout/A09-holdout-inventory.json
PASS: the durable inventory explicitly records the deficient ZH stratum and all three blockers.

git diff --check
PASS: tracked-diff whitespace. FAIL: 0. NOT-EXECUTED: no OCR, translation, cleanup, browser, renderer, or holdout evaluation was run because the holdout cannot validly freeze.
```

Before/after metrics and artifact paths: the inventory has 18 provisional candidates: 8 JA, 8 KO, and 2 ZH. JA provisionally satisfies four B&W multi-panel pages; KO provisionally supplies six multi-panel pages. ZH is two qualifying multi-panel pages short. This is a source-coverage shortfall, not a model-quality result.

Quality-gate result and reviewer: A09 cannot pass. A09 and G0 are BLOCKED. A declared-language count, two suitable ZH pages, unreviewed candidate families, and absent holdout labels/thresholds cannot satisfy the 24-page, 8-per-language, independent-family evaluation gate.

Remaining uncertainty or blocker: acquire at least two source-reviewed Chinese-dialogue multi-panel conventional-balloon pages outside the development and documented related-family clusters. Establish their creator/series family independence, then source-review labels and freeze thresholds for all 24 pages. Do not fill the gap with translated, wrong-language, related, single-panel, portrait, collage, or free-standing-text inputs.

Exact next command and expected result: after candidate source acquisition, rerun `./.venv/bin/python docs/quality-evidence/audit_style_coverage.py --output /tmp/opencode/a09-style-coverage-verify --selection <new-hash-pinned-holdout-selection.json>`; it must decode every selected input, preserve source hashes, and prove no selected ID or exact byte overlaps a development fixture. A coordinator then reviews language/script, family separation, labels, and thresholds before freezing A09.


## 2026-09-13 supplemental Chinese candidate intake

Ten user-supplied Chinese-source / human-English-reference pairs were retained as `corpus/gaps/pending/zh/sample639` through `sample648`. `sample639` also retains its provided Japanese reference. Their source and generated artifact SHA-256 values, OCR/translation element counts, Torii provenance, and app-output provenance are pinned in `corpus/gaps/pending/zh/run-20260913-curated-zh-human-pairs.json`.

Fresh app browser exports completed for all ten with non-empty PP-OCRv6 OCR and translation layers. Each saved project records `openai/gpt-5.6-luna` through OpenRouter; Torii used the same Luna model with OpenRouter BYOK. The export harness now refuses an incomplete OCR or translation stage and disables application fallback models, so an incomplete or substituted run exits without an export.

Candidate visual screening does not establish Chinese-dialogue source review, conventional multi-panel eligibility, family independence, holdout labels, or acceptance thresholds. A09 and G0 remain BLOCKED.

## 2026-09-13 candidate replacement review

Visual review replaces `sample201` (sparse dialogue) and `sample205` (one dominant scene) with four substantially stronger ZH multi-panel conventional-balloon candidates: `sample639` (five panels / 12 OCR and translation elements), `sample641` (four / 12), `sample642` (six / 15), and `sample644` (four / 9). Each retains a human English reference and a rebuilt Luna-only app project. The [candidate selection](../quality-runs/a09-20260913-holdout/A09-zh-candidate-selection.json) pins source hashes, output stage counts, selection and exclusion reasons.

`./.venv/bin/python docs/quality-evidence/audit_style_coverage.py --output docs/quality-runs/a09-20260913-holdout/source-audit --selection docs/quality-runs/a09-20260913-holdout/A09-zh-candidate-selection.json` passed: all 647 declared source entries decoded without error; the four selected sources are byte-distinct, outside the declared development and related-family exclusions, and all are `pending/zh`. Its [resolved selection](../quality-runs/a09-20260913-holdout/source-audit/resolved-selection.json) is the immutable audit result.

The audit deliberately does not prove family independence, nearby-artwork separation, native/source language beyond visible Chinese dialogue, ownership/action labels, or thresholds. It also contains only four of the eight required ZH holdout pages. Therefore it closes the *visual four-page multi-panel candidate* gap but not A09, and G0 cannot complete.

Files/worktree that must be preserved: all A01-A08 evidence and checkpoints, immutable corpus sources and references, `style-coverage-20260910/`, and the blocked A09 inventory. Do not overwrite historical baselines or generate a corpus run while G0 remains blocked.
