# A09: independent language/layout/style holdout
Status: BLOCKED

Date / app head / worker head / corpus head: 2026-09-11 / `e7b811040f6b37bc28a9b4a1f8fbf995f2bacca4` / `7b54f8c1c516eb445e869b2776ffa62e4ee2762b` / `ed9b191da255e4d3d132a2d050ab8fffa1815e26`.

Allowed files and actual changed files: `docs/quality-runs/a09-20260911-holdout/A09-holdout-inventory.json`, this checkpoint, and the tracker status update. No corpus source, source metadata, image, reference, application, worker, backend, schema, model, or submodule changed.

Prerequisites and input artifact hashes: A07 and A08 are complete. The durable [A09 inventory](../quality-runs/a09-20260911-holdout/A09-holdout-inventory.json) is pinned to current heads and candidate source SHA-256 values. The source inventory decodes 637 corpus entries with 636 distinct hashes; `sample258` / `sample637` are the sole exact duplicate pair.

2026-09-11 original reproduction (command, observed result): `./.venv/bin/python docs/quality-evidence/audit_style_coverage.py --output /tmp/opencode/a09-style-coverage-current --selection docs/quality-evidence/style-coverage-20260910/resolved-selection.json` completed with 637 decoded sources and zero decoding errors. At that point, direct enlarged-sheet review and individual source review found only `sample201` and `sample205` as candidate ZH pages outside the development set that were Chinese-dialogue, multi-panel, conventional-balloon pages. The later diversity correction below supersedes that source-selection conclusion.

GitNexus impact (repo, symbols, callers/processes, risk, freshness): not applicable. A09 is an evidence-only inventory and changes no production symbol, API, worker flow, route, source, or submodule. No implementation impact is implied by the recorded source shortfall.

Implementation decision and schema/config/model/font versions: no frozen holdout manifest, region labels, threshold set, model, or renderer exists. The retained `a09-holdout-inventory/v1` is a superseded 2026-09-11 research artifact: it holds 8 provisional JA and 8 provisional KO rows plus two ZH rows. The current Chinese candidate roster is the eight-page [diversity selection](../quality-runs/a09-20260913-holdout/A09-zh-diversity-selection.json).

Validation commands and executed/pass/fail/not-executed counts:

```text
./.venv/bin/python docs/quality-evidence/audit_style_coverage.py --output /tmp/opencode/a09-style-coverage-current --selection docs/quality-evidence/style-coverage-20260910/resolved-selection.json
PASS: 637 decoded sources, 0 source errors, 636 unique bytes, 24 development controls preserved.

jq -e '.status == "blocked" and (.shortlist.zh | length == 2) and (.blockers | length == 3)' docs/quality-runs/a09-20260911-holdout/A09-holdout-inventory.json
PASS: the durable inventory explicitly records the deficient ZH stratum and all three blockers.

git diff --check
PASS: tracked-diff whitespace. FAIL: 0. NOT-EXECUTED: no OCR, translation, cleanup, browser, renderer, or holdout evaluation was run because the holdout cannot validly freeze.
```

2026-09-11 historical result: the original inventory had 18 provisional candidates: 8 JA, 8 KO, and 2 ZH. JA provisionally satisfied four B&W multi-panel pages; KO provisionally supplied six multi-panel pages. The later eight-page ZH diversity selection supersedes the original source-coverage conclusion.

The historical family-based gate and its source-acquisition instruction are superseded. A09 remains blocked only on holdout-specific ownership/action labels, frozen quality thresholds and fresh required stage evidence. The selection verification command is now `./.venv/bin/python docs/quality-evidence/audit_style_coverage.py --output /tmp/opencode/a09-diversity-selection-verify --selection docs/quality-runs/a09-20260913-holdout/A09-zh-diversity-selection.json`; it must decode every selected input, preserve source hashes and prove no selected ID or exact byte overlaps a development fixture. A coordinator then reviews language/script, visual diversity, labels and thresholds before freezing A09.


## 2026-09-13 diversity correction

Decision: A09 is an output-quality gate. It requires language, layout and art-style diversity plus direct duplicate/development-overlap exclusions. Creator, series, uploader and translator provenance is useful optional source context, not an A09 eligibility or blocking criterion.

The [diversity selection](../quality-runs/a09-20260913-holdout/A09-zh-diversity-selection.json) restores `sample201` as a colored three-panel dialogue page and `sample205` as a sparse, painterly single-panel dialogue control. It adds `sample229`, `sample527`, `sample639`, `sample641`, `sample642` and `sample644`, for eight hash-pinned ZH candidates with six multi-panel conventional-balloon pages and two deliberately non-multi-panel style slices. `sample205` does not count toward the four-page multi-panel minimum.

The corpus-wide source audit on this checkout completed successfully: 647 sources decoded, no decode errors, and the only exact-byte duplicate remains `sample258`/`sample637`. Its contact-sheet review identifies repeated visual templates for downsampling, rather than treating absent creator attribution as a source-acquisition failure.

A09 and G0 remain BLOCKED because the candidate pages still need holdout-specific ownership/action labels, frozen quality thresholds, and fresh required stage evidence. These are quality prerequisites. No source acquisition or provenance investigation blocks the gate.

## 2026-09-13 supplemental Chinese candidate intake

Ten user-supplied Chinese-source / human-English-reference pairs were retained as `corpus/gaps/pending/zh/sample639` through `sample648`. `sample639` also retains its provided Japanese reference. Their source and generated artifact SHA-256 values, OCR/translation element counts, Torii provenance, and app-output provenance are pinned in `corpus/gaps/pending/zh/run-20260913-curated-zh-human-pairs.json`.

Fresh app browser exports completed for all ten with non-empty PP-OCRv6 OCR and translation layers. Each saved project records `openai/gpt-5.6-luna` through OpenRouter; Torii used the same Luna model with OpenRouter BYOK. The export harness now refuses an incomplete OCR or translation stage and disables application fallback models, so an incomplete or substituted run exits without an export.

## 2026-09-13 near-duplicate and source-context review

Adult material is valid private-corpus input. The [family review](../quality-runs/a09-20260913-holdout/A09-family-provenance-review.json) records creator conflicts, source context and a dHash result with no exact-byte or close visual duplicates in the original reviewed pool. It is useful curation evidence but does not decide A09 eligibility.

The read-only [Hentoid collection review](../quality-runs/a09-20260913-holdout/A09-hentoid-collection-review.json) inventoried 1,440 galleries, including 71 declared-Chinese galleries and 3,562 hashed Chinese pages. Nine exact Hentoid-to-corpus source-byte matches attribute current ZH samples to four gallery metadata artists; the new evidence resolves the earlier unattributed clusters into separate creator/style groups. It remains source context, not a gate.

Selection still down-samples repeated visual templates. The [diversity selection](../quality-runs/a09-20260913-holdout/A09-zh-diversity-selection.json) prioritizes layout, palette, text-treatment and dialogue-density coverage rather than creator attribution.

## 2026-09-13 candidate review sheets and ten-page rosters

`docs/quality-evidence/candidate-sheets-20260913/` holds 4-up review sheets: `primary-{ja,ko,zh}-NN.jpg` for the ten-page roster per language, plus `reserve-{zh,ja}-hentoid-NN.jpg`, `reserve-pixiv-NN.jpg`, `reserve-ko-xscrape-NN.jpg` and `reserve-zh-xscrape-01.jpg` for replacements. The tracked [sheet index](../quality-evidence/candidate-sheets-20260913/sheet-index.json) pins every tile tag to its source and caption; the JPGs are private previews and are gitignored. Regenerate with `./.venv/bin/python docs/quality-evidence/build_candidate_sheets.py --index docs/quality-evidence/candidate-sheets-20260913/sheet-index.json`.

The [candidate roster](../quality-runs/a09-20260913-holdout/A09-candidate-roster-20260913.json) records ten pages per language. Coordinator review rejected `sample253`: a 625x587 page with a trivial layout and source text already overflowing its dialogue box. `sample229` is confirmed as a deliberately simple but visually unique Chinese control.

The 2026-09-13 X-scrape archive supplies 33 image-backed source posts (16 KO, 9 JA, 8 ZH). It closes the Korean reserve gap that the Hentoid and Pixiv collections could not: `ko-x4`, `ko-x14` and `ko-x15` are promoted and eight further Korean pages are held in reserve. `ja-x8` and one Hentoid page complete the Japanese roster; `zh-x4` and `zh-x7` complete the Chinese roster. Only `ja-x8` is a Japanese-source page in that archive, because `ja-x1` through `ja-x7` are already English-typeset translation outputs.

Reserve screening covered 264 Japanese-declared Hentoid galleries (60 screened) and 2,163 multi-page Pixiv works (72 screened), shortlisting 12 each. Sheets and rosters are review aids; they promote nothing into the corpus, pin no labels, and run no processing. Pages without a human translation need a Torii API reference before evaluation.

The full A09 manifest remains absent: the roster now carries ten reviewed candidate pages per language, above the eight-page minimum, but it still needs holdout-specific ownership/action labels and frozen quality thresholds. A07's 267 labels are development-fixture labels and cannot be reused for a holdout. Therefore A09 and G0 remain BLOCKED.


Files/worktree that must be preserved: all A01-A08 evidence and checkpoints, immutable corpus sources and references, `style-coverage-20260910/`, and the blocked A09 inventory. Do not overwrite historical baselines or generate a corpus run while G0 remains blocked.
