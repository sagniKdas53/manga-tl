# A04 — editor and PNG/ZIP export capture

Status: `DONE`

## Superseding retained capture

The earlier blocked attempt below correctly identified that deleted A03 chapters cannot be recovered
with credentials. A new isolated retained run now resolves that lifecycle failure:
[`a03a04-20260910-retained/`](../quality-runs/a03a04-20260910-retained/). The six live chapters
were captured in the same authenticated browser session that created them, so no credential was
written to disk or needed between A03 and A04.

Each `a04-exports/<sample>/` directory contains an actual `editor.png`, browser `export.png`,
`rendered.png`, `project.zip`, unpacked `project/project.json`, `browser-fonts.json`, and
`page-snapshot.json`. All six complete sets exist. The font inventories report 38 loaded browser
faces per fixture, rather than substituting a host system-font probe. The run manifest stores every
artifact SHA-256 and page/chapter ID. `node --check`, runner help, isolated Compose health, and ZIP
unpacking all passed. A05 is ready for the visual comparison sheets and issue matrix.

## Earlier blocked attempt

A03 provides six fresh current-checkout pages in
[`docs/quality-runs/a03-20260910-fresh-admin/`](../quality-runs/a03-20260910-fresh-admin/):
`sample177`, `sample222`, `sample61`, `sample99`, `sample93`, and `sample83`.
Their chapter and page UUIDs remain in `manifest.json`.

The required existing browser helper was checked as a black box:

```text
node scripts/playwright/capture_exports.cjs --help
  PASS; options include --chapter, --out, --pages, --rendered and --keep-zip
node scripts/playwright/check.cjs --help
  BLOCKED before help output: default http://localhost:8080/tlhub was unavailable
node scripts/playwright/export_pending.cjs --help
  PASS; usage/options printed
```

## Change

Evidence-only partial run directory added:
[`docs/quality-runs/a04-20260910-editor-exports/`](../quality-runs/a04-20260910-editor-exports/).
No production source, behavior, or historical A03 artifact was changed.

## Validation / availability

The current isolated Compose project was verified at `http://localhost:18080/tlhub`:

```text
docker compose -f docker-compose.dev.yml ps
  backend healthy; db healthy; minio running; redis healthy; worker healthy
GET /tlhub/actuator/health
  HTTP 200 {"status":"UP"}
```

An access-recovery probe registered a unique disposable `translator` account via
the public API. Its generated password was held only in-process and was neither
printed nor persisted. Authenticated API results were:

```text
POST /tlhub/api/auth/register       HTTP 200; role=translator; token present
GET  /tlhub/api/auth/me             HTTP 200; role=translator
GET  /tlhub/api/series?size=200     HTTP 200; one series visible: sample83
GET  /api/series/<A03 series>/chapters?size=200
  sample177 HTTP 200, content=[]
  sample222 HTTP 200, content=[]
  sample61  HTTP 200, content=[]
  sample99  HTTP 200, content=[]
  sample93  HTTP 200, content=[]
  sample83  HTTP 200, content=[sample83 local OCR]
```

The six A03 chapter records are therefore not all present in the current
isolated database. The translator account can list/open the remaining sample83
chapter, but cannot access the other five because their series listings are
empty. The prior administrator-password probe remains unresolved and the
repository credential candidate returned HTTP 401. No PNG, ZIP, screenshot,
artifact-revision, or browser-effective-font output was created.

System font probes (not browser provenance) returned:

```text
sans-serif       DejaVu Sans / DejaVuSans.ttf
Arial            Liberation Sans / LiberationSans-Regular.ttf
Noto Sans        Noto Sans / NotoSans-Regular.ttf
Noto Sans CJK JP Noto Sans CJK JP / NotoSansCJK-Regular.ttc
```

These do not satisfy A04's effective-font requirement because the browser never
reached the editor.

## Commands/results

```text
node scripts/playwright/capture_exports.cjs --help                       PASS
node scripts/playwright/check.cjs --help                                 BLOCKED by connection refusal
node scripts/playwright/export_pending.cjs --help                         PASS
docker compose -f docker-compose.dev.yml ps                              PASS (services available on 18080)
curl http://localhost:18080/tlhub/actuator/health                        PASS (HTTP 200)
POST /tlhub/api/auth/login with repository local credential               FAIL (HTTP 401)
POST /tlhub/api/auth/register disposable translator                     PASS (HTTP 200; password in-process only)
GET /tlhub/api/auth/me authenticated                                  PASS (HTTP 200; role=translator)
GET /tlhub/api/series and six chapter listings authenticated             PARTIAL (only sample83 visible)
```

## Limitations / handoff

No helper-only substitute was used. An administrator password alone cannot
restore the five deleted baseline chapters. Recreate or rerun all six A03
projects without deleting their predecessors, then run the existing helper for
each chapter and write outputs below the reserved A04 run directory. The
authoritative tracker records this dependency mismatch. No commit was made.
