# G4: browser renderer seam

Status: **PASSED** — 2026-09-15 UR02 user approval accepted the measured single-context capacity.

E01–E06 establish one shared layout/content scene, a pinned Chromium renderer, immutable worker transport, revision-safe callback staging, an amd64 Compose service, and measured source-sized browser output. The final E05 image includes the generated static scene bundle; the first container image lacked it and returned HTTP 500 on `/render`, which E06 reproduced and corrected before measurement.

Evidence:

- [E03](E03.md): synthetic ordinary, rotation/blank and preserve rendering, diagnostics, visual Chromium proof.
- [E04](E04.md): immutable scene transport, source/result identities and C04 callback staging.
- [E05](E05.md): amd64 image, pinned browser and Comic Neue asset, Compose health/limits.
- [E06](E06.md): actual ordinary and 6764×4961 `sample93` PNGs, cold/warm time, sampled RSS, and one-context rejection.

Known limits: one CPU, one browser context, 2 GiB limit. The final image's `sample93` run completed at 6764×4961 with 824 MiB sampled peak RSS; cold/warm were 28.408/28.286 seconds. The concurrency probe rejects the second job explicitly rather than falling back or downsampling. This is a rendering-seam/capacity result only: it does not claim final cleanup quality, production throughput, font/style completeness, or a release.

UR02 approved this configuration on 2026-09-15. H03–H04 may proceed under its explicit one-context, 1 CPU / 2 GiB capacity boundary; it does not authorize higher concurrency or a broader quality/release claim.

Final milestone gates: page-scene package build passed; page-renderer build and 3 Playwright tests passed; worker Ruff, Pyright and 517 tests passed; frontend 52 Vitest files/407 tests, typecheck, and production build passed; production and development Compose configurations validate. The worker-wide formatter gate has one pre-existing unrelated formatting discrepancy; the three E04-touched files are formatted and pass its check.
